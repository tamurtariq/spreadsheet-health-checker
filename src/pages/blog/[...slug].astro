---
import { getCollection } from 'astro:content';
import Layout from '../../layouts/Layout.astro';

export const prerender = true;

export async function getStaticPaths() {
  const posts = await getCollection('blog');
  return posts.map(post => ({
    params: { slug: post.slug },
    props: { post },
  }));
}

const { post } = Astro.props;
const { Content } = await post.render();
---

<Layout title={post.data.title}>
  <article class="main">
    <header class="post-header">
      <div class="post-meta">
        <time>{post.data.pubDate.toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })}</time>
        {post.data.updatedDate && <span>Updated {post.data.updatedDate.toLocaleDateString()}</span>}
        <div class="tags">
          {post.data.tags.map(tag => <span class="tag">{tag}</span>)}
        </div>
      </div>
      <h1>{post.data.title}</h1>
      <p class="post-description">{post.data.description}</p>
    </header>

    <div class="post-content">
      <Content />
    </div>

    <footer class="post-footer">
      <hr />
      <p>Ready to validate your spreadsheets? <a href="/dashboard">Try Spreadsheet Health Checker free</a>.</p>
      <p><a href="/blog">← Back to Blog</a></p>
    </footer>
  </article>

  <style>
    .main { max-width: 800px; margin: 0 auto; padding: 3rem 1.5rem; }
    
    .post-header { margin-bottom: 3rem; }
    .post-meta {
      display: flex;
      flex-wrap: wrap;
      gap: 1rem;
      font-size: 0.875rem;
      color: #9ca3af;
      margin-bottom: 1rem;
    }
    .post-meta time { font-weight: 500; color: #6b7280; }
    .tags { display: flex; flex-wrap: wrap; gap: 0.5rem; }
    .tag {
      background: #eef2ff;
      color: #3730a3;
      padding: 0.25rem 0.75rem;
      border-radius: 9999px;
      font-size: 0.75rem;
      font-weight: 500;
    }
    
    h1 { font-size: 2.5rem; line-height: 1.2; margin: 0 0 1rem; color: #1f2937; }
    .post-description { font-size: 1.25rem; color: #6b7280; margin: 0; }
    
    .post-content { line-height: 1.8; color: #374151; }
    .post-content h2 { font-size: 1.5rem; margin: 2.5rem 0 1rem; color: #1f2937; }
    .post-content h3 { font-size: 1.25rem; margin: 2rem 0 0.75rem; color: #1f2937; }
    .post-content p { margin: 1rem 0; }
    .post-content ul, .post-content ol { margin: 1rem 0; padding-left: 1.5rem; }
    .post-content li { margin: 0.5rem 0; }
    .post-content a { color: #667eea; text-decoration: none; }
    .post-content a:hover { text-decoration: underline; }
    .post-content code { background: #f3f4f6; padding: 0.125rem 0.375rem; border-radius: 4px; font-size: 0.9em; }
    .post-content pre { background: #1f2937; color: #e5e7eb; padding: 1.5rem; border-radius: 8px; overflow-x: auto; margin: 1.5rem 0; }
    .post-content pre code { background: none; padding: 0; font-size: 0.875rem; }
    .post-content table { width: 100%; border-collapse: collapse; margin: 1.5rem 0; }
    .post-content th, .post-content td { border: 1px solid #e5e7eb; padding: 0.75rem; text-align: left; }
    .post-content th { background: #f9fafb; font-weight: 600; }
    .post-content tr:nth-child(even) { background: #f9fafb; }
    .post-content blockquote { border-left: 4px solid #667eea; padding-left: 1rem; margin: 1.5rem 0; color: #6b7280; font-style: italic; }
    
    .post-footer {
      margin-top: 3rem;
      padding-top: 2rem;
      color: #6b7280;
    }
    .post-footer a { color: #667eea; font-weight: 500; }
    
    @media (max-width: 768px) {
      .main { padding: 2rem 1rem; }
      h1 { font-size: 1.75rem; }
    }
  </style>
</Layout>